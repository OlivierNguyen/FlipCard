import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class Layout extends Component {
    render() {
        const S = {
            container: {},
        };

        return <div style={S.container}>Layout</div>;
    }
}
