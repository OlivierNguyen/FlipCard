import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class Card extends Component {
    render() {
        const S = {
            container: {},
        };

        return <div style={S.container}>Card</div>;
    }
}
