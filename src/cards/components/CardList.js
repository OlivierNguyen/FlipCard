import React, { Component } from 'react';
import PropTypes from 'prop-types';

export default class CardList extends Component {
    render() {
        const S = {
            container: {},
        };

        return <div style={S.container}>CardList</div>;
    }
}
